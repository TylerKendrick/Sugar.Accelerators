name: Project.Sdks.Common

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - Project.Sdks/Common/**/*
jobs:
  full-release:
    uses: ./.github/workflows/full-release.yml
    secrets: inherit
    with:
      path: Project.Sdks/Common
      name: 'Sugar.Accelerators.Sdks.Common'
